# Obsidian 강의 정리 지침

이 파일의 내용을 이 작업공간의 기본 작업 지침으로 사용한다.

사용자가 Discord 강의 화면의 PPT 슬라이드 이미지를 업로드하면,
단순 OCR이 아니라 복습 가능한 Obsidian 학습 노트 형태로 정리한다.

---

## 1. 기본 원칙

- 슬라이드 내용을 그대로 복사하지 말고 개념 중심으로 재구성한다.
- 슬라이드에 없는 내용을 과도하게 추가하지 않는다.
- 여러 슬라이드가 하나의 주제를 설명하면 하나의 흐름으로 연결한다.
- 중복 슬라이드는 통합한다.
- 교수자가 강조한 정의, 명령어, 옵션, 예제, 주의사항을 우선한다.
- 확실하지 않은 내용은 임의로 단정하지 않는다.
- 명령어와 코드는 반드시 코드 블록으로 작성한다.
- 중요한 개념은 **굵게** 표시한다.
- 지나치게 장황하게 설명하지 않는다.

---

## 2. Obsidian 링크 규칙

Vault에 존재하는 기존 노트와 관련 있는 경우
`[[노트명]]` 형식의 내부 링크를 사용한다.

규칙:

- 핵심 개념에만 링크한다.
- 일반 단어마다 링크하지 않는다.
- 동일한 링크를 한 문단 안에서 반복하지 않는다.
- 기존 노트가 존재하지 않는 경우 억지로 링크를 만들지 않는다.
- 기존 노트 내용을 추측하지 않는다.
- 관련 개념 노트가 존재하면 자연스럽게 연결한다.

예:

```md
`man`은 [[Linux 명령어]]의 사용법을 확인할 때 사용하는 명령어이다.

파일의 접근 권한은 [[파일 권한]]과 관련된다.
```

---

## 3. 폴더 구조

Linux 관련 노트는 다음 두 폴더로 구분한다.

```text
Linux/
├─ 개념/
└─ 예제/
```

### Linux/개념

다음과 같은 일반 학습 내용은 `Linux/개념`에 정리한다.

- 명령어 설명
- 옵션 의미
- 파일 시스템
- 사용자와 그룹
- 권한
- 프로세스
- Shell
- glob
- symbolic link
- 기타 Linux 이론 및 개념

단순히 코드나 명령어가 포함되어 있다는 이유만으로
예제 폴더로 분리하지 않는다.

### Linux/예제

**슬라이드 제목 또는 문제 표시가 `EX`로 시작하는 경우에만**
예제로 판단한다.

예:

```text
EX 사용자 보조그룹 확인
EX cat /etc/shadow 실행 오류
EX glob 문자 확인
```

이 경우만 `Linux/예제`에 별도 정리한다.

`EX` 표시가 없는 명령어 실행 화면이나 코드 조각은
`Linux/개념`에 포함한다.

---

## 4. 파일 생성 규칙

이미지 한 장마다 파일을 생성하지 않는다.

파일 생성 기준은 슬라이드 수가 아니라
**주제의 독립성**이다.

규칙:

- 같은 강의 또는 같은 주제의 슬라이드는 하나의 Markdown 문서에 통합한다.
- 연속된 슬라이드는 기본적으로 같은 노트의 연속 내용으로 간주한다.
- 하나의 강의에서 여러 소주제가 나오더라도 관련성이 높으면 `##`, `###` 헤딩으로 구분한다.
- 기존 Vault에 같은 주제의 노트가 있으면 새 파일 생성보다 기존 노트 확장을 우선한다.
- 완전히 다른 주제로 전환되었을 때만 별도 파일 생성을 고려한다.
- 이미지 파일명이나 슬라이드 번호를 파일명으로 사용하지 않는다.
- 파일명은 내용의 핵심 주제를 기준으로 정한다.

예:

잘못된 방식:

```text
slide01.md
slide02.md
slide03.md
```

권장 방식:

```text
Linux/개념/Linux 사용자와 그룹.md
```

---

## 5. EX 예제 처리

`EX`로 시작하는 슬라이드는 일반 개념 노트와 분리하여
`Linux/예제`에 정리한다.

기본 형식:

````md
# EX 예제 제목

관련 개념:
- [[관련 개념]]

## 문제

문제에서 요구하는 내용을 간단히 정리한다.

## 명령어

```bash
command
```

## 결과 / 원인

실행 결과, 오류 원인 또는 동작 이유를 설명한다.

## 핵심 포인트

- 복습할 핵심 내용
````

추가 규칙:

- 문제 문구를 그대로 복사하기보다 이해하기 쉽게 정리한다.
- 명령어는 반드시 코드 블록으로 작성한다.
- 오류가 발생하는 문제라면 오류 원인을 설명한다.
- 관련된 `Linux/개념` 노트가 있으면 `[[내부 링크]]`로 연결한다.

---

## 6. 중복 슬라이드 처리

PPT 애니메이션이나 단계별 설명 때문에
비슷한 슬라이드가 여러 장 등장할 수 있다.

이 경우:

- 같은 내용을 반복 작성하지 않는다.
- 새로 추가된 내용만 반영한다.
- 이전 슬라이드와 합쳐 하나의 설명으로 정리한다.
- 화면 일부만 달라졌다면 새로운 파일을 만들지 않는다.

---

## 7. 기본 Markdown 구조

일반 개념 노트는 다음 형식을 우선 사용한다.

````md
# 주제

## 개념

개념 설명

- 핵심 포인트
- 핵심 포인트

## 명령어

```bash
command
```

명령어 설명

## 주의사항

- 주의할 내용

## 관련 개념

- [[관련 개념]]
````

필요하지 않은 섹션은 억지로 만들지 않는다.

---

## 8. 작성 스타일

- 짧고 명확하게 작성한다.
- 복습하기 좋은 형태로 정리한다.
- 불필요한 서론과 결론을 만들지 않는다.
- 같은 설명을 반복하지 않는다.
- 명령어 옵션은 가능하면 항목별로 정리한다.
- 교수자의 설명보다 과도하게 깊은 내용으로 확장하지 않는다.
- 단순 OCR 결과가 아니라 사람이 정리한 강의 노트처럼 작성한다.

---

## 9. 작업 흐름

사용자가 여러 장의 이미지를 업로드하면 다음 순서로 처리한다.

1. 슬라이드들의 공통 주제를 파악한다.
2. 중복된 슬라이드를 제거한다.
3. 일반 개념인지 `EX` 예제인지 구분한다.
4. 적절한 폴더를 결정한다.
5. 기존 노트와 연결 가능한 개념을 확인한다.
6. 하나의 흐름으로 Markdown을 작성한다.
7. 필요한 경우에만 새 파일을 분리한다.

---

## 10. 최우선 규칙

다음 규칙은 항상 우선한다.

- 이미지 1장 = 파일 1개로 만들지 않는다.
- `EX`로 시작하는 경우에만 `Linux/예제`로 분리한다.
- 그 외 Linux 내용은 기본적으로 `Linux/개념`으로 정리한다.
- 같은 주제는 최대한 하나의 노트로 통합한다.
- 기존 Obsidian 노트와 관련 있으면 적절히 `[[내부 링크]]`를 사용한다.
